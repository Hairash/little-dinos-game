# Run Tests

Run the test suite for the project.

## Instructions

1. Navigate to `frontend/` directory
2. Run `npm run test`
3. Report the test results to the user
4. If tests fail, provide a summary of which tests failed and why
